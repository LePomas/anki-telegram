.env
data/
.venv/
__pycache__/
*.pyc
dist/
*.kate-swp

# script that generates the README demo media — regenerate locally
# with scripts/render-demo-media.sh; the media itself (assets/cover.png,
# assets/demo.webm) is committed since README.md links to it and GitHub
# renders it from the repo, not from a local-only path
scripts/render-demo-media.sh
